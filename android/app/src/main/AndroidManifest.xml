<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".SentinelApplication"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/Theme.MalmegaVilleSentinel"
        android:icon="@drawable/logo"
        android:roundIcon="@drawable/logo">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".DashboardActivity" android:exported="false" />

        <!-- Reschedules the periodic lost-device check-in after a reboot,
             since WorkManager's own scheduled jobs don't survive one on their
             own without this. -->
        <receiver android:name=".BootReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- UserPresentReceiver (unlock) and UsbConnectedReceiver (USB
             connect) are registered dynamically at runtime in
             SentinelApplication instead of declared here - USER_PRESENT and
             ACTION_POWER_CONNECTED are implicit broadcasts the OS does not
             deliver to a manifest-declared receiver since Android 8.0, so a
             static declaration here would silently never fire. -->

        <!-- Receives the "send this SMS" push from the backend and relays it
             through this phone's own SIM - see mobilePushService.ts. -->
        <service
            android:name=".SentinelFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

    </application>
</manifest>
