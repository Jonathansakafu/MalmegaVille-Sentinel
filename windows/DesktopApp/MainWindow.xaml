<Window x:Class="MalmegaVille.Sentinel.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MalmegaVille Sentinel" Height="420" Width="760">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="0,0,0,16">
            <TextBlock Text="MalmegaVille Sentinel" FontSize="24" FontWeight="Bold" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Spacing="8" Margin="0,0,0,16">
            <Button Content="Sync Now" Width="120" Click="SyncNow_Click" />
            <Button Content="Refresh Queue" Width="120" Click="RefreshQueue_Click" />
            <TextBlock x:Name="QueueStatusText" VerticalAlignment="Center" Margin="12,0,0,0" />
        </StackPanel>

        <GroupBox Header="Queued Events" Grid.Row="2">
            <Grid>
                <ListBox x:Name="QueuedEventsList" />
            </Grid>
        </GroupBox>
    </Grid>
</Window>
